/**
 * shadcn-style entry: re-exports the animated table primitives (row hover + tokens).
 * Prefer: `import { Table, TableRow, ... } from "@/components/ui/table"`
 */
export {
  Table,
  TableBody,
  TableCaption,
  TableCell,
  TableFooter,
  TableHead,
  TableHeader,
  TableRow,
  tableRowInteractiveClassName,
} from "./animated-table-rows"
