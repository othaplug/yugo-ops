/**
 * Spec alias: building profile matching lives in `matcher.ts`.
 * Prefer importing from `@/lib/buildings/matcher` or this file interchangeably.
 */
export { matchBuildingProfile } from "./matcher"
export type { BuildingProfileRow } from "./types"
