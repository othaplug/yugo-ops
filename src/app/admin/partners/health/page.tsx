export const metadata = { title: "Partner Health" };
export const dynamic = "force-dynamic";

import PartnerHealthClient from "./PartnerHealthClient";

export default function PartnerHealthPage() {
  return <PartnerHealthClient />;
}
